/*Sea a un número real positivo, considere la sucesión: x0=1, x_i+1 = 0.5 (x_i + a/x_i) i= 0,1,2,….
Puede mostrarse que x_i -> sqrt(a) cuando i -> infty. Escriba un programa que lea valores de a,
use el algoritmo para calcular la raíz de a. Detenga la iteración cuando 
    |a-x_i*x_i| < EPS , ud de el valor de EPS.*/

